name: Slack Message

on: push

jobs:
  slack-message:
    runs-on: ubuntu-latest
    steps:
      - name: Send Slack Message
        run: |
          curl -X POST -H 'Content-type: application/json' --data '{"text":"PUSHED THE COMMAND"}' ${{ secrets.SLACK_URL }}

